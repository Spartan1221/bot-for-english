# Зависимости для разработки и запуска тестов.
# Установка: pip install -r requirements-dev.txt
# (включает всё из requirements.txt через установку основных зависимостей отдельно)
pytest>=8.0.0
pytest-asyncio>=0.23.0
